# Database (Neon Postgres + pgvector)
DATABASE_URL=

# LLM Providers
ANTHROPIC_API_KEY=

# Embeddings — must match the model used by scripts/offline/index_embeddings.py
# so query-time and indexed vectors share the same space
VOYAGE_API_KEY=

# RAG engine selection: langchain | llamaindex | vercel-ai | custom
DEFAULT_RAG_ENGINE=vercel-ai
